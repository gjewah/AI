<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--
      Selvregistrering i Kontrollrom-menyen (KR v6.82-mekanismen).

      HVORFOR: Kommunikasjon var rammet av TO uavhengige feil, ikke én.
        1. `view:` på menypunktet gjorde at KR viste sin EGEN gamle innebygde utgave og aldri
           kalte runAction() → Gjermund så «testversjonen fra 14 dager siden» uansett hva vi
           leverte. Rettet av KR-økta i v6.83 (`58b868f`).
        2. DENNE: modulen hadde aldri meldt seg inn i KR-menyen. Uten innmelding finnes det
           ingen dør inn fra Kontrollrommet — uansett hvor riktig modulen ellers er.
      Fiks 1 alene er derfor ikke nok. Verifisert i `get_fiq_flater()`
      (fiq_gui_control_config.py:1240): den leser ALLE nøkler med prefikset, uansett hvilken
      modul de kommer fra → modulen melder seg inn SELV. `fiq_gui_control` røres ikke.

      Sikkerhet: `env.ref(xmlid, raise_if_not_found=False)` dropper linja hvis handlingen ikke
      finnes i basen → aldri et dødt menypunkt i en tenant som ikke har modulen.

      ⚠️ MALFORMERT JSON DROPPES STILLE (`except (ValueError, TypeError): continue`) — en
      skrivefeil gir INGEN feilmelding, bare en flate som aldri dukker opp. Verdien under er
      derfor validert maskinelt med en JSON-parser, ikke bare lest over.

      ⚠️ Dette er SERVER-SIDE KR-meny (ir.config_parameter) — en ANNEN mekanisme enn skallets
      nettleser-registry «fiq_gui_flates». Ikke bland dem.

      SEKVENS 40: Kommunikasjon er en daglig-bruk-flate og hører høyt oppe, mellom
      Deviations (35) og Timeline (45).

      KUN «Kommunikasjon» meldes inn — IKKE e-post. Gjermund 17.07.2026: «e-post skal ikke
      vises før vi er inne i kommunikasjonssenteret». E-post er en KANAL inne i flaten
      (Alle · E-post · WhatsApp · Teams · chat), ikke et eget punkt i hovedmenyen.

      Språk: `label` er ENGELSK i kilden (som de fem eksisterende oppføringene) — norsk kommer
      via i18n/*.po. Aldri hardkod norsk i modulkilde.
    -->
    <data noupdate="0">

        <record id="flate_komm" model="ir.config_parameter">
            <field name="key">fiq_gui_control.flate.komm</field>
            <field name="value">{"label": "Communication", "xmlid": "fiq_gui_comm.action_fiq_gui_comm", "sequence": 40}</field>
        </record>

    </data>
</odoo>
