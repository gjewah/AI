<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <record id="gdpr_server_action_apply_block" model="ir.actions.server">
            <field name="name">GDPR: Anvend blokkering</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="binding_model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
for partner in records:
    if partner.x_gdpr_blocked and partner.email:
        Blacklist = env['mail.blacklist'].sudo()
        if not Blacklist.search([('email', '=ilike', partner.email)], limit=1):
            Blacklist.create({'email': partner.email.lower().strip()})
    if partner.x_gdpr_blocked:
        env['mail.activity'].sudo().search([
            ('res_model', '=', 'res.partner'),
            ('res_id', '=', partner.id),
        ]).unlink()
            ]]></field>
        </record>

        <record id="gdpr_server_action_cleanup" model="ir.actions.server">
            <field name="name">GDPR: Rydding av blokkerte kontakter</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code"><![CDATA[
for partner in records:
    try:
        env['marketing.participant'].sudo().search([
            ('partner_id', '=', partner.id),
            ('state', 'not in', ['completed', 'canceled']),
        ]).write({'state': 'canceled'})
    except Exception:
        pass
    try:
        if partner.email:
            contacts = env['mailing.contact'].sudo().search([
                ('email', '=ilike', partner.email),
                ('opt_out', '=', False),
            ])
            contacts.write({'opt_out': True})
    except Exception:
        pass
            ]]></field>
        </record>

        <!-- Block contact from form/list view -->
        <record id="gdpr_server_action_block_partner" model="ir.actions.server">
            <field name="name">GDPR: Blokker kontakt</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="binding_model_id" ref="base.model_res_partner"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code"><![CDATA[
for partner in records:
    partner.apply_gdpr_block(reason='Manuell blokkering via Action', source='manual')
            ]]></field>
        </record>

        <!-- Unblock contact -->
        <record id="gdpr_server_action_unblock_partner" model="ir.actions.server">
            <field name="name">GDPR: Fjern blokkering</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="binding_model_id" ref="base.model_res_partner"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code"><![CDATA[
for partner in records:
    partner.remove_gdpr_block()
            ]]></field>
        </record>

    </data>
</odoo>
